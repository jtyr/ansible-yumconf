{{ ansible_managed | comment }}
{% for section, options in yumconf_config.iteritems() -%}
  {{ "\n" }}[{{ section }}]{{ "\n" }}
  {%- for key, value in options.iteritems() | sort -%}
    {{ key }}={{ value }}{{ "\n" }}
  {%- endfor %}
{% endfor %}
